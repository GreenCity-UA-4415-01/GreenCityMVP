# This is a basic workflow to help you get started with Actions

name: CI/CD GreenCity
# Controls when the action will run.

concurrency: Testing

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master, dev_java21 ]
  pull_request:
    branches: [ master, dev_java21 ]

env:
  repository: greencity
  userRepoName: core
  onbootJarName: app.jar
  dockerRepoName: azkeep/sofserpbl-greencitymvp

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: ubuntu-latest

    # Add permissions to allow the GITHUB_TOKEN to write Check Runs
    permissions:
      checks: write
      contents: write

    # Steps represent a sequence of tasks that will be executed as part of the job
    # Fetch depth 0 is needed for correct Sonar analysis
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

    # Checks-out your greencity.repository under $GITHUB_WORKSPACE, so your job can access it

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:

          java-version: 21
          distribution: 'temurin'
          cache: 'maven'

#       - name: Set up Go
#         uses: actions/setup-go@v3
#         with:
#           go-version: 1.18

      - name: Maven Build and Sonar Analysis
        env:
          GITHUB_TOKEN: ${{ secrets.GH_USER_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_USER }}
          PR_KEY: ${{ github.event.pull_request.number }}
          PR_BRANCH: ${{ github.head_ref }}
          PR_BASE: ${{ github.base_ref }}
#       Fetch full history for SonarQube analysis (blame, history)
#       1. Run the build, tests, JaCoCo merge, and JaCoCo aggregate report generation.
#       2. Run the Sonar scanner, adding coverage path and PR analysis properties.
#       3. Only run Sonar on PR events or when pushing to master/dev_java21
        run: |
          mvn clean install verify
          mvn org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.projectKey=GreenCity-UA-4415-01_GreenCityMVP \
            -Dsonar.organization=greencity-ua-4415-01 \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco-aggregate/jacoco.xml \
             \
            -Dsonar.qualitygate.wait=true \
             \
            -Dsonar.pullrequest.key=$PR_KEY \
            -Dsonar.pullrequest.branch=$PR_BRANCH \
            -Dsonar.pullrequest.base=$PR_BASE
        
          if: ${{ github.event_name == 'pull_request' || github.event_name == 'push' }}

#          mvn clean install verify sonar:sonar -Dsonar.projectKey=GreenCity-UA-4415-01_GreenCityMVP -Dsonar.organization=greencity-ua-4415-01

      - name: Test Reporter
        uses: dorny/test-reporter@8f8b7f8c92a9b3d0c9533f8c5b0429f9e577264a
        if: success() || failure()
        with:
          name: Tests Report
          path: ${{ env.userRepoName }}/target/surefire-reports/*.xml
          reporter: java-junit

      - name: Rename user jar to app
        run: mv $userRepoName/target/*.jar $userRepoName/target/$onbootJarName

      - name: Set outputs
        run: echo "GITHUB_SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Docker Login
        uses: docker/login-action@ef04f86d2673d328328c304d4454848074d20b6f
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker images
        uses: docker/build-push-action@2495147854d90d3d57f00d234d3d70d30c5e7144
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ env.dockerRepoName }}:test-${{ env.GITHUB_SHA_SHORT }}

#       - name: Kubernetes Set Context
#         uses: Azure/k8s-set-context@v3.0
#         with:
#           kubeconfig: ${{ secrets.KUBE_CONFIG }}
      
#       - name: Set up Postgres
#         run: kubectl scale deploy postgres --replicas=1 -n test
      
#       - name: Kubernetes tests
#         run: |
#           cd chart-test
#           go test -v ./...
      
#       - name: Post Set up Postgres
#         if: success() || failure()
#         run: kubectl scale deploy postgres --replicas=0 -n test

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Deploy 🚀
        if: ${{ github.event_name == 'push' }}
        uses: JamesIves/github-pages-deploy-action@4c6e932b137c1d1a60cc6c32c53f889984639446
        with:
          token: ${{ secrets.GH_USER_TOKEN }}
          BRANCH: gh-pages
          FOLDER: .
          CLEAN: true 
