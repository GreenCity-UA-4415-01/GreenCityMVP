# This is a basic workflow to help you get started with Actions

name: CI/CD GreenCity
# Controls when the action will run.

concurrency: Testing

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master, dev_java21 ]
  pull_request:
    branches: [ master, dev_java21 ]

env:
  repository: greencity
  userRepoName: core
  onbootJarName: app.jar
  dockerRepoName: azkeep/sofserpbl-greencitymvp

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: ubuntu-latest

    # Add permissions to allow the GITHUB_TOKEN to write Check Runs
    permissions:
      checks: write
      contents: write

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

    # Checks-out your greencity.repository under $GITHUB_WORKSPACE, so your job can access it

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:

          java-version: 21
          distribution: 'temurin'
          cache: 'maven'

#       - name: Set up Go
#         uses: actions/setup-go@v3
#         with:
#           go-version: 1.18

      - name: Run a multi-line script
        env:
          GITHUB_TOKEN: ${{ secrets.GH_USER_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_USER }}
        run: |
          git fetch --unshallow
          mvn clean install verify sonar:sonar -Dsonar.projectKey=GreenCity-UA-4415-01_GreenCityMVP -Dsonar.organization=greencity-ua-4415-01

      - name: Test Reporter
#        uses: dorny/test-reporter@v1.5.0
        uses: dorny/test-reporter@4914313f8373322f98f625902b934091c784e1b8
        if: success() || failure()
        with:
          name: Tests Report
          path: ${{ env.userRepoName }}/target/surefire-reports/*.xml
          reporter: java-junit

      - name: Rename user jar to app
        run: mv $userRepoName/target/*.jar $userRepoName/target/$onbootJarName

      - name: Set outputs
        run: echo "GITHUB_SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Docker Login
#        uses: docker/login-action@v3
        uses: docker/login-action@0d42c3c59a356075f778393e5066c1f1f2e13271
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker images
#        uses: docker/build-push-action@v5
        uses: docker/build-push-action@f2a1d5e85a0378d388339c9f0c2394d0c1537237
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ env.dockerRepoName }}:test-${{ env.GITHUB_SHA_SHORT }}

#       - name: Kubernetes Set Context
#         uses: Azure/k8s-set-context@v3.0
#         with:
#           kubeconfig: ${{ secrets.KUBE_CONFIG }}
      
#       - name: Set up Postgres
#         run: kubectl scale deploy postgres --replicas=1 -n test
      
#       - name: Kubernetes tests
#         run: |
#           cd chart-test
#           go test -v ./...
      
#       - name: Post Set up Postgres
#         if: success() || failure()
#         run: kubectl scale deploy postgres --replicas=0 -n test

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Deploy 🚀
        if: ${{ github.event_name == 'push' }}
#        uses: JamesIves/github-pages-deploy-action@v4
        uses: JamesIves/github-pages-deploy-action@83b9e4a3c1f93f1853d9d300d869769a9b2b5278
        with:
          token: ${{ secrets.GH_USER_TOKEN }}
          BRANCH: gh-pages
          FOLDER: .
          CLEAN: true 
